---
kind: Deployment
apiVersion: apps/v1
metadata:
  name: praetorium
spec:
  selector:
    matchLabels:
      app: praetorium
  template:
    metadata:
      labels:
        app: praetorium
    spec:
      serviceAccountName: praetorium
      securityContext:
        fsGroup: 1000
      containers:
        - name: praetorium
          image: shokohsc/praetorium
          ports:
            - name: http
              containerPort: 5005
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /
              port: http
