---
apiVersion: v1
kind: Service
metadata:
  name: praetorium
  labels:
    app: praetorium
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: praetorium
